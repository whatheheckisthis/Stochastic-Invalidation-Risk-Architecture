# Live governed datasets must never be committed
/data/live/*
!/data/live/.gitkeep
!/data/live/README.md
notebooks/*.ipynb filter=nbstripout

# Local dependency cache (pre-existing, not version-controlled)
iato-mcp/node_modules/
